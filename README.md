# Bare-Metal-Motion-Tracker-Using-ESP32
No machine learning. No neural networks. No external CV libraries on the MCU. Just per-pixel grayscale difference against a recursively updated background, running in IRAM on the ESP32 core.
